**Задача 12.** Да се намери средната големина на обикновен файл, който не започва с . (точка) в текущата директория (рекурсивно).

**Решение:**

``sh
gfind . -type f 2>/dev/null -printf "%s %f\n" | awk '$2 ~ "^[^.]" {c+=1;s+=$1}END{print s/c}'
``
